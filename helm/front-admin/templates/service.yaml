apiVersion: v1
kind: Service

metadata:
  name: front-admin

spec:
  type: {{ .Values.service.type }}

  selector:
    app: front-admin

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP