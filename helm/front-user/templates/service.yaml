apiVersion: v1
kind: Service

metadata:
  name: front-user

spec:
  type: {{ .Values.service.type }}

  selector:
    app: front-user

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP